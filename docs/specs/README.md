# Specifications

Behavioural specifications for hermes.

| Document | Contents |
|---|---|
| [Original spec](original-spec.md) | Behavioural snapshot of the predecessor Dart CLI (`alfred_jetbrains_cli` 2.6.2) — the baseline the Go rewrite must stay compatible with for the frozen contracts (Alfred Script Filter JSON, `alfred_*`/`jb_*` env vars). |

_New numbered specs are added per topic as the Go rewrite's own behaviour solidifies, mirroring heraut's `01-overview.md`-style convention._
